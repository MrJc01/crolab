version: '3.8'

services:
  crolab:
    build: .
    ports:
      - "8844:8844"  # Admin
      - "8855:8855"  # Client
    volumes:
      - crolab-data:/app/data
    restart: unless-stopped
    environment:
      - TZ=America/Sao_Paulo

volumes:
  crolab-data:
